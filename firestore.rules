rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  match /users/{userID} {
  	allow create: if
    	request.auth != null
  }
    match /users/{userID} {
      allow read, write: if
          request.auth != null &&
          request.auth.uid == userID
    }
  }
}